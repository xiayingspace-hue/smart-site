---
doc_type: frontend_spec
req_id: REQ-XXXX
version: 0.1.0
status: draft
generated_from: requirement.md@0.1.0
data_contract_ref: data-contract.md@0.1.0
ui_spec_ref: ui-spec.md@0.1.0
generated_at: YYYY-MM-DD
owner: ""
---

# 前端开发说明:[需求标题]

> **本文档供前端开发工程师及其 agent 使用**。
>
> ⚠️ **重要约定**:
> - 所有 API 字段定义必须**引用** data-contract.md,本文档不重复定义。
> - 所有 UI 元素引用 ui-spec.md。
> - 代码注释中必须标注覆盖的 AC ID(如 `// AC-XXXX-001`)。

---

## 0. 溯源块

| 项 | 值 |
|---|---|
| 来源需求 | REQ-XXXX @ v0.1.0 |
| 数据契约 | data-contract.md @ v0.1.0 |
| UI 设计 | ui-spec.md @ v0.1.0 |
| 覆盖 Story | US-XXX |
| 覆盖 AC | AC-XXXX-XXX |

---

## 1. 功能概述

<!-- 一段话描述本次前端要交付的内容 -->

---

## 2. 技术栈

### 2.1 已有技术栈(继承)

<!-- 列出团队既定技术栈,例如:
- 框架
- 语言
- 状态管理
- UI 库
- 路由
- 构建
- 测试
-->

### 2.2 本需求新增依赖

| 包 | 版本 | 用途 | 评估 |
|---|------|------|------|
| | | | |

---

## 3. 路由设计

| 路径 | 组件 | 权限 | 关联 Story |
|-----|------|-----|-----------|
| | | | |

**URL state 同步**:
<!-- 哪些状态需要写入 URL 以便刷新保持/可分享 -->

---

## 4. 组件结构

### 4.1 组件树

```
<[页面组件]>
├── <[子组件 A]>
└── <[子组件 B]>
    └── <[孙组件]>
```

### 4.2 关键组件说明

#### `<[组件名]>`

**关联 AC**: AC-XXXX-XXX

**Props**:
```ts
interface [ComponentName]Props {
  // ...
}
```

**职责**:
<!-- 列出本组件做什么 -->

**不该做**:
<!-- 列出明确划清的边界 -->

---

## 5. 状态管理

### 5.1 状态分层

按以下规则归类状态,**不要混淆**:

| 状态类型 | 存放位置 | 例子 |
|---------|---------|------|
| 服务端缓存 | React Query / SWR | |
| 全局 UI | Zustand / Pinia / Redux | |
| 路由/筛选 | URL query string | |
| 表单临时 | React Hook Form / VeeValidate | |
| 组件局部 | useState | |

### 5.2 缓存 Key 约定

```ts
// 统一 key 工厂,便于失效控制
export const [domain]Keys = {
  all: ['[domain]'] as const,
  lists: () => [...[domain]Keys.all, 'list'] as const,
  detail: (id: string) => [...[domain]Keys.all, 'detail', id] as const,
};
```

### 5.3 缓存失效策略

| 操作 | 失效的 key |
|-----|-----------|
| | |

---

## 6. API 接入

> 所有接口字段定义参见 `data-contract.md`,本节只列前端调用方式。

### 6.1 接口客户端生成

- **方式**: 从 OpenAPI yaml 用 `openapi-typescript` 生成 TS 类型
- **位置**: `src/api/generated/`
- **更新**: data-contract 变更时,CI 自动重新生成,类型不一致则 build 失败

### 6.2 调用清单

| API ID | 在哪些组件中调用 | 缓存策略 |
|--------|---------------|---------|
| | | |

### 6.3 错误处理

> 错误码引用 data-contract §4.2 的错误响应表。

```ts
// 全局错误映射(用户可见文案)
const errorMessages: Record<string, string> = {
  // [ERROR_CODE]: '[用户可见提示]',
};
```

---

## 7. 性能预算

| 指标 | 目标 | 测量 |
|-----|------|-----|
| 首屏 FCP | | |
| 首屏 LCP | | |
| 单 chunk size | | |
| 总 bundle | | |

**优化手段**:
<!-- 列出本需求采用的具体优化手段 -->

---

## 8. 弱网与离线策略

| 场景 | 策略 |
|-----|------|
| 请求超时 | |
| 操作中断网 | |
| 完全离线 | |

---

## 9. 错误边界与降级

<!-- 列出 ErrorBoundary 范围、降级方案、上报机制 -->

---

## 10. 埋点设计

> 对应需求 §9.5 与 §14 成功指标。

| 事件名 | 触发时机 | 关键属性 | 关联指标 |
|-------|---------|---------|---------|
| | | | |

---

## 11. 国际化

- i18n 库:<!-- react-i18next / vue-i18n -->
- 文案来源:ui-spec.md §9
- 当前支持:<!-- zh-CN -->
- 后续:<!-- en-US 等 -->

---

## 12. 测试要求

| 层级 | 框架 | 覆盖范围 | 覆盖率目标 |
|-----|------|---------|-----------|
| 单元 | | | |
| 集成 | | | |
| E2E | | | |

> ⚠️ E2E 测试代码归 QA 维护,前端只跑单元和集成。

---

## 13. AC 覆盖检查表

| AC ID | 实现位置(文件/组件) | 测试覆盖 | 状态 |
|------|------------------|---------|------|
| AC-XXXX-XXX | | | TODO |

---

## 14. 与后端的联调约定

- **联调前置**: data-contract.md 已锁版
- **Mock 服务**: 基于 data-contract 自动生成,前端可独立开发
- **联调环境**: dev 环境
- **接口变更**: 任何变更必须先改 data-contract,再通知前后端

---

## 15. 验收条件

前端开发完成的判定:

- [ ] 所有 AC 在 §13 表中标记完成
- [ ] 单元 + 集成测试通过,覆盖率达标
- [ ] E2E 测试由 QA 跑通(本项不阻塞前端提测)
- [ ] Lighthouse 性能分数达标
- [ ] 无 console 错误/警告
- [ ] 无障碍自动化检查无 critical 项
- [ ] 已部署到 dev 环境,可联调

---

## 16. 变更历史

| 版本 | 日期 | 修改人 | 变更摘要 |
|-----|------|-------|---------|
| 0.1.0 | YYYY-MM-DD | | 初稿 |
